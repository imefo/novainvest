<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <title>NovaInvest — سرمایه‌گذاری هوشمند (نسخه نمایشی)</title>

  <!-- SEO -->
  <meta name="description" content="NovaInvest — نسخه نمایشی برای نمایش پلن‌های سرمایه‌گذاری در هوش مصنوعی و بازارهای مالی. پلن ماهانه ۱۲٪، ثبت‌نام، داشبورد و محاسبه‌گر سود.">
  <meta name="theme-color" content="#7c4dff">

  <!-- Open Graph / Social Preview -->
  <meta property="og:title" content="NovaInvest — سرمایه‌گذاری هوشمند">
  <meta property="og:description" content="پلن ماهانه ۱۲٪ (نمایشی)، ثبت‌نام، داشبورد و محاسبه‌گر سود.">
  <meta property="og:type" content="website">
  <meta property="og:url" content="https://novainvest.netlify.app/">

  <!-- Favicon (SVG inline) -->
  <link rel="icon" href='data:image/svg+xml;utf8,
  <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 256 256">
    <rect width="256" height="256" rx="48" fill="#0d0d12"/>
    <path d="M64 192V64l96 96V64" fill="none" stroke="%237c4dff" stroke-width="28" stroke-linecap="round" stroke-linejoin="round"/>
    <circle cx="200" cy="56" r="12" fill="%23d4af37"/>
  </svg>'>

  <style>
    :root{
      --bg:#0d0d12; --panel:#151521; --text:#e8e8f3; --muted:#a4a4bf;
      --accent:#7c4dff; --gold:#d4af37; --ring:rgba(124,77,255,.3);
      --radius:14px;
    }
    *{box-sizing:border-box}
    body{margin:0;font-family:Tahoma, Arial, sans-serif;background:var(--bg);color:var(--text)}
    a{color:inherit;text-decoration:none}
    header{position:sticky;top:0;z-index:10;background:#0f0f17cc;border-bottom:1px solid rgba(255,255,255,.06);backdrop-filter:blur(8px)}
    .container{max-width:1080px;margin:0 auto;padding:20px}
    .nav{display:flex;gap:16px;justify-content:space-between;align-items:center}
    .brand{display:flex;gap:10px;align-items:center;font-weight:800}
    .logo{width:34px;height:34px;border-radius:10px;background:radial-gradient(120% 120% at 20% 20%, var(--accent) 0%, #4227a6 45%, #1a1430 100%); box-shadow:0 0 0 6px rgba(124,77,255,.12), 0 8px 30px rgba(124,77,255,.35)}
    .nav a{padding:10px 12px;border-radius:10px;color:var(--muted)}
    .nav a:hover{background:rgba(255,255,255,.06);color:#fff}
    .btn{border:1px solid rgba(255,255,255,.15);background:#191926;color:#fff;padding:10px 14px;border-radius:12px;cursor:pointer}
    .btn.primary{background:var(--accent);border-color:transparent}
    .hero{padding:36px 20px}
    .grid{display:grid;gap:16px}
    .grid-2{grid-template-columns:repeat(2,1fr)}
    .grid-3{grid-template-columns:repeat(3,1fr)}
    @media(max-width:900px){.grid-2,.grid-3{grid-template-columns:1fr}}
    .card{background:linear-gradient(180deg, rgba(255,255,255,.04), rgba(255,255,255,.02));border:1px solid rgba(255,255,255,.08);border-radius:var(--radius);padding:18px}
    .title{font-size:28px;margin:0 0 8px}
    .muted{color:var(--muted)}
    .section{padding:10px 20px}
    .section h2{margin:0 0 10px}
    .kpi{display:flex;flex-direction:column;gap:6px}
    .kpi .val{font-size:22px;font-weight:800}
    .plan-title{font-weight:800}
    .plan-rate{font-size:24px;color:var(--accent);font-weight:800}
    input,select,textarea{width:100%;background:#1a1a2b;border:1px solid rgba(255,255,255,.12);color:#fff;border-radius:10px;padding:12px;outline:none}
    input:focus,select:focus,textarea:focus{box-shadow:0 0 0 4px var(--ring)}
    label{display:block;margin-bottom:6px}
    footer{margin-top:40px;border-top:1px solid rgba(255,255,255,.08);padding:18px;text-align:center;color:var(--muted)}
    .chip{display:inline-block;background:rgba(124,77,255,.15);border:1px solid rgba(124,77,255,.35);padding:6px 10px;border-radius:999px;font-size:12px}
  </style>
</head>
<body>

<header>
  <div class="container nav">
    <div class="brand"><span class="logo"></span> NovaInvest</div>
    <nav>
      <a href="#plans">پلن‌ها</a>
      <a href="#calc">محاسبه‌گر</a>
      <a href="#register">ثبت‌نام</a>
      <a href="#login">ورود</a>
      <a href="#dashboard">داشبورد</a>
      <a href="#contact">تماس</a>
    </nav>
  </div>
</header>

<main class="container">
  <section class="hero grid grid-2">
    <div>
      <div class="chip">سرمایه‌گذاری در هوش مصنوعی و بازارهای مالی</div>
      <h1 class="title">رشد هوشمند سرمایه با NovaInvest</h1>
      <p class="muted">این نسخه صرفاً برای تست رابط کاربری است. پرداخت واقعی ندارد.</p>
      <div style="display:flex;gap:10px;margin-top:10px">
        <a class="btn primary" href="#plans">مشاهده پلن‌ها</a>
        <a class="btn" href="#register">شروع کنید</a>
      </div>
    </div>
    <div class="card grid grid-2">
      <div class="kpi"><span class="muted">میانگین نرخ (ماهانه)</span><span class="val">۱۲٪</span></div>
      <div class="kpi"><span class="muted">حداقل سرمایه</span><span class="val">100 USDT</span></div>
      <div class="kpi"><span class="muted">پرداخت سود</span><span class="val">پایان دوره</span></div>
      <div class="kpi"><span class="muted">وضعیت</span><span class="val">نمایشی</span></div>
    </div>
  </section>

  <section id="plans" class="section">
    <h2>پلن‌های سرمایه‌گذاری</h2>
    <div class="grid grid-3">
      <div class="card">
        <div class="plan-title">پلن «هوش مصنوعی»</div>
        <div class="plan-rate">۱۲٪ / ماه</div>
        <ul class="muted" style="line-height:1.9">
          <li>حداقل سرمایه: 100 USDT</li>
          <li>مدت: 1 ماه</li>
          <li>نوع سود: ساده</li>
          <li>پرداخت سود: انتهای دوره</li>
        </ul>
        <div style="display:flex;gap:8px;margin-top:10px">
          <button class="btn primary" onclick="buyPlan()">خرید نمایشی</button>
          <a class="btn" href="#calc">محاسبه‌گر</a>
        </div>
      </div>
      <div class="card muted">پلن بعدی (TBD)</div>
      <div class="card muted">پلن بعدی (TBD)</div>
    </div>
  </section>

  <section id="calc" class="section">
    <h2>محاسبه‌گر سود</h2>
    <div class="card">
      <div class="grid grid-3">
        <div>
          <label>مبلغ (USDT)</label>
          <input type="number" id="cAmt" value="100" min="0">
        </div>
        <div>
          <label>نرخ ماهانه (%)</label>
          <input type="number" id="cRate" value="12" min="0" max="100">
        </div>
        <div>
          <label>مدت (ماه)</label>
          <input type="number" id="cMonths" value="1" min="1">
        </div>
      </div>
      <div style="margin-top:10px">
        <label>نوع سود</label>
        <select id="cType">
          <option value="simple" selected>ساده</option>
          <option value="compound">مرکب</option>
        </select>
      </div>
      <button class="btn primary" style="margin-top:10px" onclick="calc()">محاسبه</button>
      <p id="cOut" class="muted" style="margin-top:10px"></p>
    </div>
  </section>

  <section id="register" class="section">
    <h2>ثبت‌نام (لوکال)</h2>
    <div class="card">
      <div class="grid grid-2">
        <div>
          <label>نام و نام‌خانوادگی</label>
          <input id="rName" placeholder="علی رضایی">
        </div>
        <div>
          <label>Email</label>
          <input id="rEmail" type="email" placeholder="you@example.com">
        </div>
        <div>
          <label>رمز عبور</label>
          <input id="rPass" type="password" placeholder="********">
        </div>
        <div>
          <label>موبایل</label>
          <input id="rPhone" placeholder="09xxxxxxxxx">
        </div>
      </div>
      <button class="btn primary" style="margin-top:10px" onclick="register()">ایجاد حساب (لوکال)</button>
      <p class="muted" style="margin-top:6px">* اطلاعات فقط در دستگاه شما ذخیره می‌شود.</p>
    </div>
  </section>

  <section id="login" class="section">
    <h2>ورود (لوکال)</h2>
    <div class="card grid grid-2">
      <div>
        <label>Email</label>
        <input id="lEmail" type="email">
      </div>
      <div>
        <label>رمز عبور</label>
        <input id="lPass" type="password">
      </div>
      <button class="btn primary" onclick="login()">ورود</button>
      <p id="loginOut" class="muted"></p>
    </div>
  </section>

  <section id="dashboard" class="section">
    <h2>داشبورد</h2>
    <div class="grid grid-3">
      <div class="card"><div class="muted">موجودی کل</div><div id="dBal" style="font-size:28px;font-weight:800">0 USDT</div></div>
      <div class="card"><div class="muted">پلن‌های فعال</div><div id="dPlans" style="font-size:28px;font-weight:800">0</div></div>
      <div class="card"><div class="muted">سود ماه جاری</div><div id="dPnl" style="font-size:28px;font-weight:800">0 USDT</div></div>
    </div>
  </section>

  <section id="contact" class="section">
    <h2>تماس با ما</h2>
    <!-- Netlify Forms -->
    <form name="contact" method="POST" data-netlify="true" class="card" style="max-width:640px">
      <input type="hidden" name="form-name" value="contact">
      <div class="grid grid-2">
        <div>
          <label>نام</label>
          <input name="name" required>
        </div>
        <div>
          <label>ایمیل</label>
          <input type="email" name="email" required>
        </div>
      </div>
      <div style="margin-top:10px">
        <label>پیام</label>
        <textarea name="message" rows="4" required></textarea>
      </div>
      <button type="submit" class="btn primary" style="margin-top:10px">ارسال پیام</button>
      <p class="muted" style="margin-top:6px">* پیام‌های ارسال‌شده در بخش Forms نتلیفای نمایش داده می‌شوند.</p>
    </form>
  </section>
</main>

<footer>
  © <span id="year"></span> NovaInvest — تمامی حقوق محفوظ است.
</footer>

<script>
  // تنظیمات نمایشی
  const ST = { users: [], session: null, balance: 0, plans: [], txs: [] };
  const LS_KEY = "novainvest_demo_v1";
  const RATE = 0.12; // 12%/mo
  const PLAN_MIN = 100;

  function save(){ localStorage.setItem(LS_KEY, JSON.stringify(ST)); render(); }
  function load(){
    try{ Object.assign(ST, JSON.parse(localStorage.getItem(LS_KEY)||"{}")); }catch(e){}
    render();
  }
  function register(){
    const n = document.getElementById('rName').value.trim();
    const e = document.getElementById('rEmail').value.trim();
    const p = document.getElementById('rPass').value;
    const ph = document.getElementById('rPhone').value.trim();
    if(!n || !e || !p) return alert('فیلدهای ضروری را تکمیل کنید.');
    if(ST.users.find(u=>u.e===e)) return alert('این ایمیل قبلاً ثبت شده.');
    ST.users.push({ n, e, p, ph });
    ST.session = e;
    alert('ثبت‌نام انجام شد (لوکال).');
    save();
  }
  function login(){
    const e = document.getElementById('lEmail').value.trim();
    const p = document.getElementById('lPass').value;
    const u = ST.users.find(u=>u.e===e && u.p===p);
    const out = document.getElementById('loginOut');
    if(u){ ST.session = e; out.textContent='ورود موفق ✅'; save(); }
    else { out.textContent='ایمیل یا رمز نادرست است.'; }
  }
  function buyPlan(){
    if(!ST.session) return alert('ابتدا وارد شوید.');
    const price = PLAN_MIN;
    if(ST.balance < price) return alert('موجودی کافی نیست. ابتدا واریز کنید.');
    ST.balance -= price;
    const plan = { name:'AI-1M', months:1, rate:RATE, principal:price, start: Date.now() };
    ST.plans.push(plan);
    ST.txs.unshift({ t:'BUY_PLAN', amt: -price, ts: Date.now(), meta: plan.name });
    alert('پلن فعال شد (نمایشی).');
    save();
  }
  function calc(){
    const A = parseFloat(document.getElementById('cAmt').value||0);
    const r = parseFloat(document.getElementById('cRate').value||0)/100;
    const m = parseInt(document.getElementById('cMonths').value||1);
    const type = document.getElementById('cType').value;
    const final = (type==='simple') ? A*(1+r*m) : A*Math.pow(1+r,m);
    const profit = final - A;
    document.getElementById('cOut').textContent = `سود تقریبی: ${profit.toFixed(2)} USDT — مبلغ نهایی: ${final.toFixed(2)} USDT`;
  }
  function render(){
    document.getElementById('year').textContent = new Date().getFullYear();
    document.getElementById('dBal').textContent = ST.balance.toFixed(2)+' USDT';
    document.getElementById('dPlans').textContent = ST.plans.length;
    const pnl = ST.plans.reduce((s,p)=> s + p.principal * p.rate, 0);
    document.getElementById('dPnl').textContent = pnl.toFixed(2)+' USDT';
  }
  load();
</script>

</body>
</html>
